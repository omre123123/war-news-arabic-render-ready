services:
  - type: web
    name: war-news-arabic
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/news
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
