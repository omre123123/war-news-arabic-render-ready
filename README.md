# WarRoom X Cloudflare Worker

هذه النسخة مخصصة لـ Cloudflare Workers/Pages وتدعم:
- /api/news
- /api/economy
- /api/debug
- واجهة من public

## Cloudflare settings
Build command:
npm install

Deploy command:
npx wrangler deploy

Output directory:
public
